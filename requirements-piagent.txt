# Direct dependencies for PiAgent development.
#
# For reproducible Docker/dev installs, use:
#   pip install -r requirements-piagent.lock.txt
#
# Keep this file as the human-maintained dependency intent, and regenerate
# the lock file after intentional upgrades.
langchain==1.2.10
langgraph==1.0.10
langchain-openai==1.1.10
langchain-anthropic==1.3.4
python-dotenv==1.0.1
colorama==0.4.6
sqlite-vec==0.1.6

# Development verification dependencies used by the documented Docker workflow.
pytest==8.2.2
pytest-asyncio==0.26.0
